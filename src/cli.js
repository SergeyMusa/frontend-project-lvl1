//import readlineSync from 'readline-sync';

// module.exports = {
export const Greeting = (nameYou) => {

        let hi = `Hello, ${nameYou}!`;
        console.log(hi);
        return (hi);
}
export default Greeting;